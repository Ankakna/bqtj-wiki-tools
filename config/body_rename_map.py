# 角色重命名映射表
# 格式: 英文ID -> 新的中文名（语义化命名）
# 说明: 用于手动调整角色的 cnName。当原始数据中的中文名存在格式问题、
#       同名冲突或需要语义化命名时，在此表中配置。
# 使用: 在 parse_body.py 中自动应用，无需额外操作。
# 顺序: 按需添加，建议与 data/body/处理报告.txt 中重名组顺序一致以便对照。

BODY_RENAME_MAP = {
    'XiaoMei': '天鹰小美',
    'WatchdogAir': '飞天狗',
}
